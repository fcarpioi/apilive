# Firebase Functions

## Flags de entorno

- `CLEANUP_EVENT_COPERNICO`: habilita/deshabilita la limpieza diaria de `stories` y `participants` para los eventos "Maratón" y "Medio Maratón".
  - `true` = activa la limpieza
  - `false` (o no definido) = desactiva la limpieza

